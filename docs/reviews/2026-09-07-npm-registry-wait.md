# npm 发布后可见性等待修复

日期：2026-09-07。层级：单 SDK 的发布流程。

## 问题与修改

1.2.0 发布运行 `34129623612` 的 `npm publish` 已成功，但 npm registry 随后的五次查询均返回 404。查询从 13:54:36 UTC 到 13:54:50 UTC，原脚本总等待约 14 秒，早于 npm 完成公开处理。后续已另行核验公开包及来源证明；历史失败运行保持原状态。

发布后的只读查询改为最多 21 次，间隔 30 秒，允许十分钟的可见性等待。单次 npm 网络请求超时为 15 秒，关闭 npm 内层重试，整个步骤上限为 17 分钟。查询成功立即记录完整性并结束；持续失败仍返回非零，在 Actions 摘要中区分“发布已返回成功”和“发布后核验未完成”，提示单独核验 registry 与来源证明，不要重跑发布或重复发布。

本次不修改 SDK、模型或已发布版本，不重新发布 npm，不移动任何标签。

## 回归与验证

- 新增测试从实际 release 工作流抽取 Bash 脚本执行，只替换外部 npm 查询和等待。立即可见、延迟三分钟可见、持续不可见三种行为均覆盖。修复前后两项失败变为三项全部通过。
- `pnpm release:test`：25 项通过，包括发布契约、包构建和三项等待回归。
- 文档 6 项、基准契约 5 项、基准比较 14 项、SDK 89 项、独立示例 12 项、Demo 26 项通过；lint、类型检查和 SDK/Demo/示例构建通过。
- 修改前后标准检查各 18 项 required 通过、0 失败、4 项远程规则跳过。仅代表本地检查结果。
- 修改文件的 Prettier 与补丁空白检查单独执行。完整 `pnpm run verify` 仍被历史 `work/pytest-basetemp-20260815` 目录的扫描权限错误阻断；保留该目录，其余阶段逐项执行，不将其描述为完整命令一次通过。干净 CI 结果须在 PR 上另行核验。

本次测试没有执行 npm publish；不能用模拟延迟证明下一次真实 npm 发布已经成功。
